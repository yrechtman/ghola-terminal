 NEVER create branches. Always work directly on main.
- NEVER open PRs. Commit and push directly to main.
- NEVER ask about branching strategy. There is none.
- Keep git simple: add, commit, push to main. That is it.
- If main has diverged, just force push.
